version: '3.8'
services:
  db:
    image: postgis/postgis:15-3.4
    environment:
      - POSTGRES_DB=rada
      - POSTGRES_USER=rada
      - POSTGRES_PASSWORD=rada
    ports:
      - "5432:5432"
    volumes:
      - rada_db_data:/var/lib/postgresql/data

volumes:
  rada_db_data: